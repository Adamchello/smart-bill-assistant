---
import { AppShell } from "@/shells/app/shell"
import AppLayout from "@/kernel/layouts/layout.astro"

export const prerender = true

export const getStaticPaths = () => {
  return [{ params: { slug: undefined } }]
}
---

<AppLayout title="App">
  <AppShell client:load />
</AppLayout>
